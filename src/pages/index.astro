---
import Layout from "../layouts/Base.astro"
import { getExamples } from "../utils/index.js"

export const prerender = true

const examples = await getExamples("latest")
---

<Layout examples={examples} />
