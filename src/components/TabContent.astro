---
export type Props = {
	id: string
	default?: boolean
}

const currentTabId = Astro.url.searchParams.get("tab")
---

{
	(currentTabId === Astro.props.id ||
		(currentTabId === null && Astro.props.default)) && <slot />
}
